# GPU-Optimized Requirements for H100/Production
# This file is only used when building with GPU support

# Base requirements (same as current)
fastapi==0.104.1
uvicorn==0.24.0
python-multipart==0.0.6
requests==2.31.0
pydantic==2.5.0
aiofiles==23.2.1

# Fallback CPU whisper (for compatibility)
openai-whisper==20231117


# System monitoring
psutil==5.9.6

# GPU-Accelerated Whisper (10-100x faster than CPU)
faster-whisper==0.10.0

# PyTorch with CUDA support
torch>=2.0.0
torchaudio>=2.0.0

# Speaker Diarization (offline-capable)
pyannote.audio>=3.1.0
pyannote.core>=5.0.0
pyannote.database>=5.0.0
pyannote.metrics>=3.2.0

# Audio processing for diarization
scipy>=1.10.0
scikit-learn>=1.3.0
speechbrain>=0.5.0

# Optional: Specific CUDA version for H100
# torch==2.1.0+cu121 --index-url https://download.pytorch.org/whl/cu121
# torchaudio==2.1.0+cu121 --index-url https://download.pytorch.org/whl/cu121

# Memory optimization for large models
memory-profiler==0.61.0

# Enhanced audio processing
librosa==0.10.1
soundfile==0.12.1 